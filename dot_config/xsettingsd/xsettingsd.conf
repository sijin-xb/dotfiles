Net/IconThemeName "Adwaita-Matugen-A"
# 光标主题：XWayland 程序（如 LinuxQQ）通过 XSETTINGS 拿光标，
# 不设就回退到默认光标，跟 Wayland 原生程序不一致。
# 必须与 dms/cursor.kdl 的 xcursor-theme / xcursor-size 保持一致。
Gtk/CursorThemeName "Matugen-Cursors"
Gtk/CursorThemeSize 32
