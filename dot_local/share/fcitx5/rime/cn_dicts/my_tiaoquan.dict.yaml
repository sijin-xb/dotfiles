# Rime dictionary
# encoding: utf-8
#
# 个人提权词库：放「词典里有、但权重低得翻不到」的字词。
#
# ── 挂载方式 ────────────────────────────────────────────────
#   在 rime_ice.dict.yaml 的 import_tables 里，必须排在 cn_dicts/8105 **之前**。
#   8105 里有 `了 liao 999`，而 import_tables 的规则是「有重复词条时最上面的
#   权重生效」——排后面就白写了。
#
# ── 权重 ↔ 位置（liao 音，8105.dict.yaml 实测权重）────────────
#   聊 265573 > 料 263518 > 疗 94630 > 辽 47836 > 廖 31067 > 撩 21011
#   当前 100000 → 「了」落在料之后、疗之前。
#   想排到「疗」之后（更保守）：权重改 60000 左右。
#
# ── 实测记录（librime 1.17.0，page_size=9，max_homophones=8）──
#   改前：「了」不在候选内（不限上限时它在完整列表第 21 位）
#   改后：1聊 2料 3了 4李敖 5利奥 6疗 7辽 8廖 9撩
#
# ── 注意事项 ────────────────────────────────────────────────
#   rime-ice-git 升级会覆盖 rime_ice.dict.yaml，届时 import_tables 里
#   这一行会丢失，重新加回即可（本文件不受影响）。

---
name: my_tiaoquan
version: "2026-09-22"
sort: by_weight
use_preset_vocabulary: false
...

了	liao	100000
